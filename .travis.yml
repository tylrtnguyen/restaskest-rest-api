language: node_js
node_js:
    - "stable"
cache:
    directories:
        - node_modules
before_install:
    - "npm config set spin false"
install:
    - npm install
script:
    - npm run build
deploy:
    - provider: heroku
    - api_key: $HEROKU_TOKEN
    - app: restaskest-api